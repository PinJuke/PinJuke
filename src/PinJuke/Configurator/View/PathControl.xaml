<view:BaseControl x:Class="PinJuke.Configurator.View.PathControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:PinJuke.Configurator.View"
    xmlns:view="clr-namespace:PinJuke.View"
    mc:Ignorable="d" 
    d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox VerticalContentAlignment="Center" Text="{Binding Path, Mode=TwoWay}"/>
        <Button Grid.Column="1" Margin="10,0,0,0" Content="{Binding ChooseText}" Click="Choose_Click"/>
        <CheckBox Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Center" Content="{Binding RelativePathText}"
                  IsChecked="{Binding Relative, Mode=TwoWay}" IsEnabled="{Binding RelativeEnabled}"/>
    </Grid>
</view:BaseControl>
